dependancies:

roll_mean
segmented
# "devetools" = require(devtools), # not shure I need that one (it is loaded anyway with Rcpp)
# "dplyr" =     require(dplyr),
# "RcppRoll" =  require(RcppRoll),
# "segmented" = require(segmented),
# "Rcpp" =      require(Rcpp))

todo:

use @seealso:
use \code{} when relevant.
use `` for mathematical expression
use @examples

use \describe{
#'  \item{p} probabilities of exceedance
#'  \item{Q} corresponding streamflow values
#' }

